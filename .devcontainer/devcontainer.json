{
  "name": "fluxq (real Fluxion + river/Postgres)",
  "dockerFile": "Dockerfile",
  "context": "..",
  "customizations": { "vscode": { "extensions": ["golang.go"] } },
  "postCreateCommand": "make db-up && echo 'real Fluxion: make fluxion; run: make run'"
}
